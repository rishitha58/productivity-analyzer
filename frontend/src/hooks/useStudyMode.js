import { useStudyMode as useStudyModeContext } from '../context/StudyModeContext';

// Re-export from context for cleaner imports
export const useStudyMode = useStudyModeContext;

export default useStudyMode;