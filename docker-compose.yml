version: '3.8'

services:
  mongodb:
    image: mongo:7
    container_name: productivity-mongo
    restart: always
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db
    environment:
      - MONGO_INITDB_DATABASE=productivity-analyzer

volumes:
  mongo-data: